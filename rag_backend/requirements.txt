# Vernacular FD Advisor — RAG backend
#
# Supported: Python 3.12 and 3.13 (Apple Silicon + macOS).
# Python 3.14+ is NOT supported yet — old pydantic-core / PyO3 builds fail.
# Create the venv with:  conda create -n vfd-advisor python=3.12 -y
#                    or:  /opt/homebrew/bin/python3.12 -m venv .venv
#
# (Historical note: torch==2.3.0 was dropped — use a current torch from PyPI.)

fastapi>=0.115.0,<0.116
uvicorn[standard]>=0.32.0,<0.35
python-multipart>=0.0.9,<0.1
python-dotenv>=1.0.1,<2
pydantic>=2.10.0,<3
pydantic-settings>=2.6.0,<3
httpx>=0.27.0,<0.29

# Document Processing
pypdf>=4.2.0,<5
python-docx>=1.1.2,<2
markdown>=3.6,<4

# Embeddings & ML
sentence-transformers>=3.0.1,<4
torch>=2.6.0,<3
faiss-cpu>=1.8.0,<2

# Retrieval
rank-bm25==0.2.2

# Language
langdetect==1.0.9
indic-transliteration==2.3.57
nltk>=3.8.1,<4

# Reranking
transformers>=4.41.0,<5
